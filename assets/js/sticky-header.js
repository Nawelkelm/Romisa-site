// Al hacer scroll posiciona la barra de navegacion arriba
//$(window).scroll(function () {
   // if ( $(this).scrollTop() > 200) {
	//	$('.navbar').addClass('sticky-top');
  //  } else {
	//	$('.navbar').removeClass('sticky-top');
  //  }
  //});